/**
 * Dark mode has been intentionally removed.
 * This component is kept as a harmless no-op to avoid accidental re-introduction via old imports.
 */
export default function DarkModeToggle() {
  return null
}
